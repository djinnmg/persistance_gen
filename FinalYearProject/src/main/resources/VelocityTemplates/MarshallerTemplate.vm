package ${package}.jaxb.serialization;

#foreach ($entity in entities)
	import ${package}.hibernate.entities.${entity.name};
	import ${package}.jaxb.types.${entity.name}Type;
#end
	
public class Marshaller
{

	#foreach ($entity in $entities)
		public ${entity.name}Type marshall($entity.name ${entity.name.toLowerCase})
		{
			${entity.name}Type ${entity.name.toLowerCase}Type = new ${entity.name}Type();
	
			#foreach ($property in $entity.properties)
				${entity.name.toLowerCase}Type.${property.name} = ${entity.name.toLowerCase}.${property.name};			
			#end
			
			return ${entity.name}Type;
		}
	#end

}
